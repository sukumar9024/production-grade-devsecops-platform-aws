AWS_REGION={{ aws_region }}
APP_SECRET_ARN={{ app_secret_arn }}
DATABASE_SECRET_ARN={{ database_secret_arn }}
DATABASE_HOST={{ database_host }}
DATABASE_NAME=secureops
REDIS_SECRET_ARN={{ redis_secret_arn }}
REDIS_HOST={{ redis_host }}
ENVIRONMENT={{ application_environment }}
S3_BUCKET={{ application_bucket }}
ECR_REGISTRY={{ ecr_registry }}
BACKEND_REPOSITORY={{ backend_repository | default('secureops-' + platform_environment + '-backend') }}
FRONTEND_REPOSITORY={{ frontend_repository | default('secureops-' + platform_environment + '-frontend') }}
LOG_GROUP_NAME={{ log_group_name | default('/secureops/secureops-' + platform_environment + '/application') }}
OPENSEARCH_HOST={{ opensearch_endpoint | default('') }}
TARGET_GROUP_ARN={{ target_group_arn | default('') }}
APP_INSTANCE_ID={{ inventory_hostname }}
CORS_ORIGINS={{ cors_origins | default('') }}
