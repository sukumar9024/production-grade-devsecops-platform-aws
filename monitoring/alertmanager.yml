# Local receiver intentionally has no external notification side effects.
# Production requires scripts/configure-observability.py and a webhook secret.
route:
  receiver: local
  group_by: [alertname, instance]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: local
